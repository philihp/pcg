name: tests

on: push

env:
  CI: true

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version:
          - 22.x
          - 24.x
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm install
      - run: npm run build --if-present
      - run: npm test
      - run: npm run lint

  compat:
    runs-on: ubuntu-latest
    needs: tests
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 16.x
          - 18.x
          - 20.x
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24.x
      - run: npm install
      - run: npm run build
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
      - name: Smoke-test built dist (CJS)
        run: |
          node -e "const p = require('./dist/index.js'); const s = p.createPcg32(42n, 54n); const [n, s2] = p.randomInt(0, 100, s); if (typeof n !== 'number') process.exit(1); JSON.stringify(p.nextState(s2)); const s64 = p.createPcg64({}, 42n, 54n); const [n64, s64b] = p.randomInt64(0, 100)(s64); if (typeof n64 !== 'number') process.exit(1); JSON.stringify(p.nextState64(s64b));"
      - name: Smoke-test built dist (ESM)
        run: |
          node --input-type=module -e "import('./dist/index.mjs').then(p => { const s = p.createPcg32(42n, 54n); const [n, s2] = p.randomInt(0, 100, s); if (typeof n !== 'number') process.exit(1); JSON.stringify(p.nextState(s2)); const s64 = p.createPcg64({}, 42n, 54n); const [n64, s64b] = p.randomInt64(0, 100)(s64); if (typeof n64 !== 'number') process.exit(1); JSON.stringify(p.nextState64(s64b)); })"

  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24.x
      - run: npm install
      - run: npm run build --if-present
      - run: npm run test:coverage
      - name: Coveralls
        uses: coverallsapp/github-action@master
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
